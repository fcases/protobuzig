// Fixture R8: packed repeated (binario) y control no-packed.
message PackedMsg {
  repeated int32 path = 1 [packed = true];
  repeated int32 simple = 2;
  repeated double vals = 3 [packed = true];
}
